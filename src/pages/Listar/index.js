import React, { Component } from 'react';

import './styles.css';

export default class Listar extends Component {
    render() {
        return (
            <div id="main-container">
                <h1>Usuários</h1>
            </div>
        );
    }
}
